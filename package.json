{
  "name": "heyhank-monorepo",
  "version": "0.94.0",
  "private": true,
  "description": "HeyHank monorepo (root). The published npm package lives in web/.",
  "scripts": {
    "dev": "cd web && bun run dev",
    "build": "cd web && bun run build",
    "start": "cd web && bun run start",
    "prepare": "husky"
  },
  "author": "Markus Stoeger",
  "license": "MIT",
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
